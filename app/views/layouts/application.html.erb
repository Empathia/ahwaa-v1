<!DOCTYPE html>
<html>
<head>
  <title>Lgbt</title>
  <%= stylesheet_link_tag :all %>
  <%= csrf_meta_tag %>
  <%= yield(:head) %>
</head>
<body>
  <% flash.each do |key, msg| %>
    <div class="flash <%= key %>"><%= msg %></div>
  <% end %>
<%= yield %>
</body>
</html>
