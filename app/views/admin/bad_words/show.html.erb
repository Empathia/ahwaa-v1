<h1><%= label_tag :bad_word_list %></h1>
<%= form_tag admin_bad_words_path, :method => :put do %>
  <p class="field">
    <%= text_area_tag :bad_word_list, @bad_word_list %>
    <span class="desc" style="display: block;">Comma separated</span>
  </p>
  <p><%= submit_tag %></p>
<% end %>
